import type { Config } from 'tailwindcss'

export default {
  theme: {
    extend: {
      fontFamily: {
        lato: ['Lato', 'sans-serif'],
        roboto: ['Roboto','times-new-roman']
      },
    },
  },
} satisfies Config
